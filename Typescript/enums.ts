const enum SeatChoice{ // Adding const here because, if we dont add const, it will generate lots of javascript code
    AISLE = "aisle",
    MIDDLE = 3,
    WINDOW,
    FOURTHSEAT
}

const selectedseat = SeatChoice.AISLE

export{}